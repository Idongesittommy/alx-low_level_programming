#include "main.h"
/**
 *print_alphabet - print the lowercase alphabet followed by a newline
 *
 */
void print_alphabet(void);

{

char la
for (la = 'a'; la <= 'z'; la++)
putchar(la);
putchar('\n')

}
